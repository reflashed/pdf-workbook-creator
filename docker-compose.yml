version: '3'

services:
  main:
    build:
      context: '.'
      dockerfile: 'Dockerfile'
    ports:
      - '3000:3000'
      - '8000:8000'
    volumes:
      - '.:/app'
      - './example-volume:/mounted-volume'
    environment:
      - 'NODE_ENV=development'
      #- DEBUG
